let FoodMachine = (/** @type {Registry.Item} */ event) => {
	/*event.create('oven', 'simple', 0, GTValues.LV, GTValues.MV, GTValues.HV) // 
        .rotationState(RotationState.NON_Y_AXIS)
        .recipeType('test_recipe_type')*/
	
	
	
}